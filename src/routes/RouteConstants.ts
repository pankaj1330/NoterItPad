const LoginRoutes = {
    LOGIN_PAGE : "/login"
}

const SignupRoutes = {
    SIGNUP_PAGE : "/signup"
}

const HomeRoutes = {
    HOME_PAGE : "/notes"
}

export const RouteConstants = {
    LoginRoutes,
    SignupRoutes,
    HomeRoutes
}